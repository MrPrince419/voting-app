{
  "name": "voting-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm run clean && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "vite": "^4.5.9",
    "rimraf": "^5.0.1"
  }
}
