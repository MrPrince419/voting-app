{
  "name": "voting-app",
  "version": "1.0.0",
  "description": "A modern, reactive voting application built with vanilla JavaScript",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server -o",
    "prebuild": "mkdir -p dist",
    "build": "cp -r index.html styles.css VotingApp.js demo-data.js src dist/ && cp -r node_modules/@fortawesome dist/node_modules/@fortawesome",
    "build:preview": "npm run build",
    "build:branch": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "voting",
    "polls",
    "javascript",
    "vanilla-js"
  ],
  "author": "MrPrince419",
  "license": "MIT",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
