{
  "name": "voting-app",
  "version": "1.0.0",
  "description": "A modern, reactive voting application built with vanilla JavaScript",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server -o",
    "prebuild": "mkdir -p dist",
    "build": "cp -r index.html styles.css scripts.js assets dist/",
    "build:preview": "npm run build",
    "build:branch": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "voting",
    "polls",
    "javascript",
    "vanilla-js"
  ],
  "author": "MrPrince419",
  "license": "MIT",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.0.0",
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.10"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
